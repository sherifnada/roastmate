setup:
  addons:
  - plan: heroku-postgresql
    as: DATABASE
build:
  docker:
    web: Dockerfile
run:
  web: pwd; ls; ls .venv/bin; poetry env info; poetry run sanic --help;
#release:
#  command:
#    - ./release.sh
#  image: web
