setup:
  addons:
  - plan: heroku-postgresql
    as: DATABASE
build:
  docker:
    web: Dockerfile
run:
  web: pwd; ls; poetry env info; poetry shell; sanic --help;
#release:
#  command:
#    - ./release.sh
#  image: web
